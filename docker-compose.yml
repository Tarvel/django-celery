version: '3.8'

services:

  redis:
    image: redis:alpine

  django:
    container_name: django
    build:
      context: ./dcelery
    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - ./dcelery:/usr/scr/app
    ports:
      - 8001:8000
    
    environment:
      - DEBUG=1
      - SECRET_KEY=13njk3243k4h2u2hukerhf2of2emff3}$F4[R4R@{]2}2e2e2e]2e2rq0090rmf
      - ALLOWED_HOSTS=localhost,127.0.0.1
    
    depends_on:
      - redis